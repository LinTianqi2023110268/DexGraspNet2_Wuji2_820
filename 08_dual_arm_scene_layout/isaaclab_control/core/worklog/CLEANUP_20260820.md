# Repository cleanup manifest — 2026-08-20

This manifest was written before cleanup.  The current production behavior is
frozen: cleanup may remove generated material and archive superseded sources,
but it must not alter task semantics, planning thresholds, physics parameters,
or robot assets.

## Protected: never move or edit in this cleanup

- `02_training_dataset/TRAIN_SCENE_INPUT_PATHS_AND_OBJECTS.txt` (the user's
  scene-address list).
- `02_training_dataset/data/`, dataset assets, checkpoints, training results,
  and verified cases.
- Official Wuji2 and dual-arm URDF/USD assets.
- The user's existing dirty-worktree changes.

## Keep: production mainline

- `run_closed_loop.sh` and
  `08_dual_arm_scene_layout/isaaclab_control/closed_loop/orchestrator.py`.
- Persistent Isaac client/worker and launchers.
- Scene migration assertions and calibrated layout/configuration.
- The authoritative `RobotDepthCleaner.remove_robot()` implementation,
  GroundedSAM adapter, perception safety gates, and color-sort package.
- Shared DGN2, LEAP-to-Wuji2, endpoint, Route A, and Route B production code.
- Route B front-half, full seven-segment pipeline, right-arm-only contract, and
  the non-versioned `trajectory_visualizer/` package.
- Training, inference, retargeting, and verified-baseline source trees.

## Keep: regression tests

- Semantic target safety, stale-mask, SourceZone, and SAM residual tests.
- Closed-loop/core unit tests.
- Route B 7DOF, collision-policy, attachment, goal-pool, and trajectory tests.
- Existing training/inference tests and compact verified fixtures.

## Keep: audit and diagnostic tools

- `closed_loop/tools/audit_scene_migration_contract.py`.
- `closed_loop/tools/audit_scene_spawn_vs_settled.py`.
- Current collision, frame, physics, capture, and route-contract diagnostics.
- Worklogs and compact JSON evidence that records resolved root causes.

## Move to external archive

Destination:
`/home/lin/DexGraspNet2_Wuji2_cleanup_archive_20260820/repository_cleanup/`.

| Source | Reason | Production replacement / reference search |
|---|---|---|
| `DexGraspNet2_Wuji2_flexible_persistent_update/` | One-time overlay package from an earlier integration | Current files under `08_dual_arm_scene_layout/isaaclab_control/`; no production reference |
| `_backup_before_flexible_persistent_v2/` | Pre-integration backup | Current closed-loop mainline; no production reference |
| `_backup_before_mount_y016_20260818/` | Manual layout rollback snapshot | Current calibrated layout plus git/worklog history; only worklog references |
| `curobo_motion_planning_routeB_v1/` | Old top-level Route B package | `08_dual_arm_scene_layout/isaaclab_control/curobo_motion_planning_routeB/`; no production reference |
| `robot_segmentation_v1/` | Old top-level RobotSegmenter package | `08_dual_arm_scene_layout/isaaclab_control/perception/robot_segmentation/`; no production reference |
| `.../curobo_motion_planning_routeB/routeB_trajectory_visualizer_v1/` | Byte-for-byte duplicate package | Adjacent non-versioned `trajectory_visualizer/`, which is imported by the production bundle builder |

Two files were already archived by the earlier pre-experiment cleanup and are
kept as deletions in the worktree:

- `closed_loop/persistent_isaac/worker.py.bak_before_recovery_patch`.
- `closed_loop/scripts/grounded_sam_backend_template.py`.

## Delete: generated output

- Root diagnostic `*.log` files from 2026-08-19; conclusions are in worklogs.
- `trimesh/outputs/` and other small inference/layout generated-output trees.
- Failed closed-loop sessions `20260820_101901` and `20260820_102515`.
- Non-selected candidate scratch cases from retained Route B baselines.
- Route/DGN scratch from the superseded color-sort run while retaining its
  current-cycle RGB/robot-mask/HSV evidence.

Retain these compact evidence families:

- `outputs/closed_loop_sessions/20260820_073606`: last seven-segment Route B
  planning PASS (candidate 1536; retain selected case and plan reports).
- `outputs/closed_loop_sessions/20260820_063033`: transfer-attachment root-cause
  evidence (candidate 7938; retain selected case and comparative reports).
- `outputs/closed_loop_sessions/20260820_091607` and `20260820_091931`:
  robot-false-positive perception evidence.
- `outputs/closed_loop_sessions/20260820_104319`: current color/HSV evidence
  only; remove downstream failed planning scratch.
- `outputs/pre_experiment_prefight/20260820_100016_semantic_curobo_perception_smoke`.
- The two formal corrected scene-migration audits.

## Delete: caches

- `__pycache__/`, `*.pyc`, `*.pyo`, and `.pytest_cache/` outside protected
  dataset/vendor/environment trees.

## Reference safety check

Before archiving, `git grep` found no production import or launcher reference to
the legacy packages.  The only remaining textual backup reference is the
historical command log.  After cleanup, production files will be searched again
for every archived/deleted path and the regression suite will be rerun.
