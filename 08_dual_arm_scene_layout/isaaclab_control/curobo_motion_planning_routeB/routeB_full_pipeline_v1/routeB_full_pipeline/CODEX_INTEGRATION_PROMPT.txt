GPT has written the Route B full-pipeline core at:

08_dual_arm_scene_layout/isaaclab_control/
curobo_motion_planning_routeB/
routeB_full_pipeline_v1/routeB_full_pipeline/

Read README.md and config_patch_example.json first.
Do not redesign the algorithms.

==================================================
A. Preserve Route A exactly
==================================================

--motion-route legacy must keep the current behavior.
Do not modify the existing PersistentScene.execute(), execute_segment(),
candidate_rfs_v2, or legacy plan_flexible_route semantics.

All new Route B execution must be additive.


==================================================
A2. Route A task semantics to reuse exactly
==================================================

For Route B, copy/reuse the **same task-state semantics** already used by Route A:

- PREGRASP endpoint comes from the existing relaxed PREGRASP legal region selected in Route B front-half;
- COVER is the exact post-retarget COVER target already screened by strict IK;
- GRASP keeps the arm at COVER and moves only Wuji2 hand to the existing GRASP waypoint;
- SQUEEZE keeps the arm at COVER and uses the existing `squeeze_dense_q20_path` 41-point Wuji2 trajectory;
- LIFT target candidates use the existing Route A `sample_lift` policy;
- TRANSFER target candidates use existing `sample_transfer`;
- PLACE/lower-point candidates must use the existing Route A `free_placement_centres_xy` + `sample_place_from_centres` policy and the same green placement zone / occupied-centre registry;
- RELEASE keeps arm at PLACE and opens hand to existing pregrasp/open hand state;
- RETREAT candidates use existing `sample_retreat`;
- HOME is the existing configured home_q_deg.

Do NOT redesign these task endpoints.
The only major Route B change is that arm motion between the chosen endpoints is generated by cuRobo MotionPlanner dense trajectories instead of Route A's quintic arm interpolation.

The additive Route B executor already reproduces Route A-style COVER/GRASP endpoint refinement, lift threshold verification, and final green-zone verification. Do not remove those checks.

==================================================
B. Route B prerequisite
==================================================

Current front-half is already PASS and generates:

cycle_root/routeB_front_half/planning/
  trajectory_right_arm.npz
  routeB_front_half_plan.npz
  routeB_front_half_report.json

The front report contains selected candidate/case/q_PREGRASP/q_COVER.

Do not regress the front-half implementation.

==================================================
C. Build back-half endpoint chain pool
==================================================

After front-half PASS, while using a CuroboWorkerClient for endpoint IK only,
call:

from routeB_full_pipeline import (
    build_backhalf_chain_pool,
    save_backhalf_chain_pool,
)

pool = build_backhalf_chain_pool(
    client=curobo,
    project_root=root,
    case_root=selected_case_root,
    q_cover_rad=np.asarray(front_report["selected"]["q_cover_rad"]),
    measured=measured,
    placement_registry=registry,
    config=cfg,
    chain_limit=cfg["routeB_full_pipeline"]["backhalf_chain_limit"],
)

If pool.chain_count == 0, continue/fail normally; do not invent a path.

Save:

cycle_root/routeB_full/backhalf_chain_pool.npz

Important: this pool does only legal LIFT/TRANSFER/PLACE/RETREAT endpoint IK.
It must NOT call legacy check_joint_path or any old path collision gate.

Release this CuroboWorkerClient before launching the heavyweight full MotionPlanner backend.

==================================================
D. Run GPT full MotionPlanner backend
==================================================

Call:

from routeB_full_pipeline import run_full_motion_backend

full_report = run_full_motion_backend(
    project_root=root,
    capture_dir=capture_root,
    query=query,
    case_root=selected_case_root,
    front_half_dir=cycle_root / "routeB_front_half/planning",
    backhalf_pool=cycle_root / "routeB_full/backhalf_chain_pool.npz",
    output_dir=cycle_root / "routeB_full/planning",
    settings=cfg["routeB_full_pipeline"],
)

The backend owns:
- target proxy from object_physics_audit.json
- filtered_depth_no_target.npy
- true 7DOF planner reconstruction
- separate hand-lock planner configurations
- official AttachmentManager sphere fit/update
- PREGRASP->COVER MotionPlanner
- COVER->LIFT MotionPlanner in non-target ESDF (target proxy detached for intentional support contact)
- LIFT->TRANSFER MotionPlanner with attached target proxy
- TRANSFER->PLACE MotionPlanner in non-target ESDF (target proxy detached for intentional placement contact)
- PLACE->RETREAT->HOME MotionPlanner with placed target cuboid
- scene/cspace constraints
- dense trajectory artifacts

Do not reimplement this inside orchestrator.py.

Expected outputs:

cycle_root/routeB_full/planning/
  routeB_execution_manifest.json
  routeB_full_plan_report.json
  traj_pregrasp_to_cover.npz
  traj_cover_to_lift.npz
  traj_lift_to_transfer.npz
  traj_transfer_to_place.npz
  traj_place_to_retreat.npz
  traj_retreat_to_home.npz

The current->PREGRASP trajectory is reused from front-half.

==================================================
E. Merge config
==================================================

Merge the `routeB_full_pipeline` block from config_patch_example.json into:

08_dual_arm_scene_layout/isaaclab_control/closed_loop/config/closed_loop.json

Do not delete any legacy config block.

==================================================
F. Persistent Isaac client: additive Route B method
==================================================

In closed_loop/persistent_isaac/client.py add:

execute_routeB(
    manifest_path,
    output_dir,
    target_segmentation_id,
)

It sends:
{
  "op": "execute_routeB",
  "manifest_path": ...,
  "output_dir": ...,
  "target_segmentation_id": ...
}

Do not alter existing execute().

==================================================
G. Persistent Isaac worker: additive Route B protocol
==================================================

Make the GPT package importable from worker.py, then import:

from routeB_full_pipeline import execute_routeB_manifest

Add only an additive protocol branch:

elif op == "execute_routeB":
    result = execute_routeB_manifest(
        scene,
        project_root=PROJECT_ROOT,
        manifest_path=Path(req["manifest_path"]),
        output_dir=Path(req["output_dir"]),
        target_segmentation_id=int(req["target_segmentation_id"]),
        quintic=quintic,
    )
    emit({"ok": True, "op": "execute_routeB", **result})

Do not modify the existing `execute` Route A path.

==================================================
H. Orchestrator semantics
==================================================

For --motion-route curobo:

1. run existing Route B front-half until PASS;
2. build backhalf endpoint chain pool;
3. run full MotionPlanner backend;
4. if --planning-only:
      print Route B FULL PLAN PASS and return 0;
5. if --sim-execute:
      call isaac.execute_routeB(...).

For --motion-route legacy: unchanged.

User commands become:

./run_closed_loop.sh --motion-route curobo --planning-only

and

./run_closed_loop.sh --motion-route curobo --sim-execute

No --experimental-bypass-planner-collision and no --no-planner-collision-check are needed for Route B.

==================================================
I. Route B collision policy is fixed
==================================================

LEAP target-reach coarse IK            OFF collision
Exact COVER old observed-ESDF gate     OFF
Relaxed PREGRASP endpoint collision    OFF
legacy HOME->PRE path gate             NOT USED
legacy PRE->COVER path gate            NOT USED
current->PREGRASP environment          ON
PREGRASP->COVER non-target environment ON
COVER->LIFT non-target environment     ON (target detached)
LIFT->TRANSFER + attached target        ON
TRANSFER->PLACE non-target environment ON (target detached)
post-release + placed target           ON
self collision                         OFF
Isaac/PhysX                            ON

Do not weaken the true MotionPlanner collision checks.

==================================================
J. Local API adaptation allowed
==================================================

You may fix only local integration/API mismatches such as:
- public vs _src cuRobo import paths
- MotionPlanner/ToolPose exact accessor name
- extra_links LinkParams schema if local installed version differs slightly
- CuroboWorker scope/lifetime
- array dtype/shape
- SceneCfg mixed cuboid+voxel construction
- output path names
- import/PYTHONPATH

If a genuine core algorithm bug is found, stop and report the traceback + minimal needed change instead of redesigning it.

Do not change:
- task sampling ranges/tolerances
- Route B MotionPlanner params
- joint limits
- URDF/USD
- ESDF voxel semantics
- VoxelData shape fix
- hand q20 values
- Route A

==================================================
K. Validation sequence
==================================================

First run static tests:

python -m py_compile <all modified/new Python files>
python -m unittest routeB_full_pipeline.test_attachment_proxy
python -m unittest routeB_full_pipeline.test_robot_config
existing Route B tests
git diff --check

Then run FULL planning only:

cd ~/Projects/DexGraspNet2_Wuji2
./run_closed_loop.sh --motion-route curobo --planning-only

Do not start full physical execution until all seven arm motion segments are present and routeB_full_plan_report.json success=true.

Then run ONE full GUI Isaac execution:

./run_closed_loop.sh --motion-route curobo --sim-execute

Do not use --isaac-headless for the first execution.

If physical execution fails because the actual grasp does not hold the object,
report the exact stage/lift/error measurements. Do not silently change hand parameters or planner policy.

==================================================
K2. Required terminal output
==================================================

The command line must visibly report progress. At minimum print:

Route B FULL PLANNING:
- selected candidate / case
- selected back-half chain
- for each arm segment: stage, point_count, dt, duration, min clearance, scene_collision max, cspace max
- target proxy dimensions and fitted sphere count
- planning PASS/FAIL

Route B EXECUTION:
- each state start/progress: CURRENT_TO_PREGRASP, PREGRASP_TO_COVER, COVER, GRASP, SQUEEZE, COVER_TO_LIFT, LIFT_TO_TRANSFER, TRANSFER_TO_PLACE, RELEASE, PLACE_TO_RETREAT, RETREAT_TO_HOME
- arm tracking max error during dense segments
- object lift during execution
- COVER/GRASP endpoint refinement PASS/FAIL
- lift threshold PASS/FAIL
- final green-zone PASS/FAIL
- final report path

Do not hide normal progress only in log files.

==================================================
L. Final report
==================================================

After the first full attempted execution report:
1. planning-only PASS/FAIL
2. selected candidate / case
3. all arm segment points, dt, duration, min clearance, scene_collision, cspace
4. target proxy dims and fitted sphere count
5. selected back-half chain index
6. full Isaac execution PASS/FAIL
7. verified lift mm
8. final green-zone PASS/FAIL
9. planning and execution report paths
10. Route A unchanged

Keep debugging ordinary path/API/scope problems until completion.
